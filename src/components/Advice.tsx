import { ReactNode } from "react"

interface IAdviceProps {
    isDragging: boolean, 
    children: ReactNode
}

export default function Advice({isDragging, children}: IAdviceProps ) {
  return (

  )
}
